{
  "name": "rfid-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "pm2start": "set -a && . ./.env.deploy && set +a && pm2 start ecosystem.config.cjs",
    "loadUrls": "set -a && . ./.env.deploy && set +a",
    "db pull": "set -a && . ./.env.deploy && set +a && DATABASE_URL='$PERSONAL_DATABASE_URL' npx prisma db pull",
    "db pull shared": "set -a && . ./.env.deploy && set +a && DATABASE_URL='$SHARED_DATABASE_URL' npx prisma db pull"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^6.19.3",
    "fastify": "^5.8.5",
    "fastify-print-routes": "^4.1.0",
    "pino-pretty": "^13.1.3",
    "pm2": "^7.0.1"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^6.5.0"
  }
}
